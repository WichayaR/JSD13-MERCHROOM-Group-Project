import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export default function RevenueTrendChart({ data = [] }) { if (!data.length) return <p>No revenue data yet.</p>; return <div className="chart"><ResponsiveContainer width="100%" height={205}><AreaChart data={data}><defs><linearGradient id="revenue" x1="0" x2="0" y1="0" y2="1"><stop stopColor="#6252CD" stopOpacity=".45" /><stop offset="1" stopColor="#6252CD" stopOpacity="0" /></linearGradient></defs><CartesianGrid vertical={false} stroke="#eeeae1" /><XAxis dataKey="month" tickLine={false} axisLine={false} /><YAxis tickLine={false} axisLine={false} width={45} /><Tooltip formatter={(value) => `฿${Number(value).toLocaleString()}`} /><Area type="monotone" dataKey="revenue" stroke="#6252CD" strokeWidth={3} fill="url(#revenue)" /></AreaChart></ResponsiveContainer></div>; }
