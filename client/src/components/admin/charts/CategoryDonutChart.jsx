import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
const colors = ['#FF4D23', '#6252CD', '#2E9E56', '#E2B800'];
export default function CategoryDonutChart({ data = [] }) { const total = data.reduce((sum, item) => sum + item.value, 0); if (!data.length) return <p>No category sales yet.</p>; return <div className="donut-wrap"><ResponsiveContainer width="48%" height={180}><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={48} outerRadius={75} paddingAngle={3}>{data.map((item, index) => <Cell key={item.name} fill={colors[index % colors.length]} />)}</Pie><Tooltip formatter={(value) => `฿${Number(value).toLocaleString()}`} /></PieChart></ResponsiveContainer><div className="category-list">{data.map((item, index) => <div key={item.name}><i className={`dot d${index % 4}`} /><span>{item.name}</span><b>{total ? Math.round(item.value / total * 100) : 0}%</b></div>)}</div></div>; }
