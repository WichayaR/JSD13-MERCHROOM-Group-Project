import { useEffect, useState } from 'react';
import { createProduct, deleteProduct, getProducts, updateProduct } from '../../src/api/products.api';
import { getProductOptions } from '../../src/api/dashboard.api';
import AdminPanel from '../../src/components/admin/AdminPanel';
import AdminTable from '../../src/components/admin/AdminTable';
const blank = { name: '', description: '', price: '', quantity: '', category: '', artist: '', imageUrl: '', tags: '' };
export default function ProductManagement() { const [products, setProducts] = useState([]); const [options, setOptions] = useState({ categories: [], artists: [] }); const [form, setForm] = useState(null); const [error, setError] = useState(''); const load = () => getProducts().then((data) => setProducts(data.products)).catch((e) => setError(e.message)); useEffect(() => { load(); getProductOptions().then(setOptions).catch((e) => setError(e.message)); }, []); const save = async (event) => { event.preventDefault(); try { const body = { ...form, price: Number(form.price), quantity: Number(form.quantity), tags: form.tags ? form.tags.split(',').map((tag) => tag.trim()).filter(Boolean) : [] }; const data = form._id ? await updateProduct(form._id, body) : await createProduct(body); setProducts((items) => form._id ? items.map((item) => item._id === data.product._id ? data.product : item) : [data.product, ...items]); setForm(null); } catch (e) { setError(e.message); } }; const edit = (item) => setForm({ ...item, category: item.category?._id || item.category || '', artist: item.artist?._id || item.artist || '', tags: item.tags?.join(', ') || '' }); return <div className="admin-page"><AdminPanel title="Product Management" action={<button className="primary-btn" onClick={() => setForm(blank)}>Add product</button>}>{error && <p className="form-error">{error}</p>}<AdminTable columns={['Product', 'Artist', 'Category', 'Price', 'Stock', '']} >{products.map((item) => <tr key={item._id}><td>{item.name}</td><td>{item.artist?.name || '—'}</td><td>{item.category?.name || '—'}</td><td>฿{Number(item.price).toLocaleString()}</td><td>{item.quantity}</td><td><button className="link-btn" onClick={() => edit(item)}>Edit</button><button className="link-btn danger" onClick={async () => { if (window.confirm('Delete this product?')) { try { await deleteProduct(item._id); setProducts((all) => all.filter((p) => p._id !== item._id)); } catch (e) { setError(e.message); } } }}>Delete</button></td></tr>)}</AdminTable></AdminPanel>{form && <div className="modal"><form className="product-form" onSubmit={save}><button type="button" className="close" onClick={() => setForm(null)}>×</button><h2>{form._id ? 'Edit product' : 'Add product'}</h2><input required placeholder="Name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /><textarea placeholder="Description" value={form.description || ''} onChange={(e) => setForm({ ...form, description: e.target.value })} /><input required type="number" min="0" placeholder="Price" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} /><input required type="number" min="0" placeholder="Quantity" value={form.quantity} onChange={(e) => setForm({ ...form, quantity: e.target.value })} /><select required value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })}><option value="">Category</option>{options.categories.map((item) => <option key={item._id} value={item._id}>{item.name}</option>)}</select><select value={form.artist} onChange={(e) => setForm({ ...form, artist: e.target.value })}><option value="">No artist</option>{options.artists.map((item) => <option key={item._id} value={item._id}>{item.name}</option>)}</select><input placeholder="Image URL" value={form.imageUrl || ''} onChange={(e) => setForm({ ...form, imageUrl: e.target.value })} /><input placeholder="Tags, separated by commas" value={form.tags} onChange={(e) => setForm({ ...form, tags: e.target.value })} /><button className="primary-btn">Save product</button></form></div>}</div>; }
